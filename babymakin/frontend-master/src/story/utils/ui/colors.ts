import { COLORS } from "src/design-system";

const {
  characteristicsTagBackgroundColor,
  promisingPracticeTagBackgroundColor,
  successSignTagBackgroundColor,
} = COLORS.story;

export const TAG_COLORS = {
  successSign: successSignTagBackgroundColor,
  characteristic: characteristicsTagBackgroundColor,
  promisingPractice: promisingPracticeTagBackgroundColor,
};
