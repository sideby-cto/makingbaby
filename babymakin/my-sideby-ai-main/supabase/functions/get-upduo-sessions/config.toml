
# Allow the function to be called from the browser
allowed_http_methods = ["POST", "OPTIONS"]

# Allow specific secrets to be accessible to this function
[secrets]
UPDUO_CLIENT_SECRET = "UPDUO_CLIENT_SECRET"
