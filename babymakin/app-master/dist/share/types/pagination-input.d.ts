export declare class PaginationInput {
    currentPage: number;
    pageSize: number;
    filter?: string | undefined;
    constructor(currentPage: number, pageSize: number, filter?: string | undefined);
}
